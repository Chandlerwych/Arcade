# Chandlers-Arcade

```
let winningCombinations = "XXX"
let arr = []
function getRows(matrix){

  // returns all your row


}
function transpose(matrix){
  // from rows to columns and columes to rows
  // return this matrix
}
function Dia(){
  // garb diagnol
}
function winner (){
  arr.map(combo =>
    if(combo === "XXX"){
      return "X is a winner"
    }else if(combo==="OOO"){
    return "O is a winner "
    }
         )
}

let row = ["X","X","X"]


let str_winner = row.join('')

if(str_winner === "XXX"){
  console.log("Winner")
}
```
